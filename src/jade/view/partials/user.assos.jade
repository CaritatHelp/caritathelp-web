.friends-tab
	.col-md-3(ng-repeat="asso in vm.user.assos")
		.card.summary
			a.summary--picture(ui-sref="association.home({id: {{asso.id}}})")
				img.picture(ng-src="http://api.caritathelp.me{{asso.thumb_path}}", alt="{{asso.name}}")
			a.summary-name(ui-sref="association.home({id: {{asso.id}}})") {{asso.name}}
			.summary--stats
				a.stat.
					Location
					#[h5.nb {{asso.city}}]
				a.stat(ui-sref="association.members({id:{{asso.id}}})").
					Membres
					#[h5.nb {{asso.nb_members}}]
	.col-md-6.col-md-push-3(ng-if="!vm.user.assos.length && vm.isCurrent")
		.card Vous n'êtes pas encore membre d'une association ! Utilisez la barre de recherche pour en retrouver.
	.col-md-6.col-md-push-3(ng-if="!vm.user.assos.length && !vm.isCurrent")
		.card Cet utilisateur n'est encore membre d'aucune association !
