.calendar
	.col-md-4
		.card.calendar--nav
			h5.title A propos
			.text-list
				.link(ng-class="{active:calendar.isSet(1)}")
					i.fa.fa-calendar-check-o
					a(ng-click="calendar.setTab(1)") Evènements
				.link(ng-class="{active:calendar.isSet(2)}")
					i.fa.fa-calendar
					a(ng-click="calendar.setTab(2)") Tous les évènements
				.link(ng-if="calendar.calType == 'association'", ng-class="{active:calendar.isSet(3)}")
					i.fa.fa-calendar-plus-o
					a(ng-click="calendar.setTab(3)") Créer un évenement
	.col-md-8
		.calendar--main

			//-Affichage de l'ensemble des évenements auxquels l'utilisateur peut participer
			.calendar--all(ng-show="calendar.isSet(2)")
				h4.title Tous les évènements
				//-Affichage d'un évenement
				.card.event(ng-repeat="event in calendar.events")
					header
						h6.event--title {{event.title}}
						.event--creator Organisé par <a href="#/associations/{{event.assoc_id}}">{{event.assoc_id}}</a>
					section.
						Du {{event.begin}} au {{event.end}}<br>
						Lieu: {{event.place}}<br>
						54 participants, dont <strong>{{event.nb_friends_members}}</strong> amis
					footer
						.event--status(ng-if="event.rights == 'admin'") Vous êtes administrateur de cet évènement
						.event--status(ng-if="event.rights == 'host'") Vous êtes le créateur de cet évènement
						.event--status(ng-if="event.rights == 'member'") Vous participez à cet évènement
						.event--status(ng-if="!event.rights") Vous ne participez pas à cet évènement
						a.btn.btn-small.btn-primary(href="#/event/{{event.id}}").
							#[.fa.fa-info-circle]
							Plus d'informations

			//- Evenements auxquels l'utilisateur participer
			.calendar--joined(ng-show="calendar.isSet(1)")
				h4.title Participation
				//-Affichage d'un évenement (même que le précédent)
				.card.event(ng-repeat="event in calendar.joined")
					header
						h6.event--title {{event.title}}
						.event--creator Organisé par <a href="#/associations/{{event.assoc_id}}">{{event.assoc_id}}</a>
					section.
						Du {{event.begin}} au {{event.end}}<br>
						Lieu: {{event.place}}<br>
						54 participants, dont <strong>{{event.nb_friends_members}}</strong> amis
					footer
						.event--status(ng-if="event.rights == 'admin'") Vous êtes administrateur de cet évènement
						.event--status(ng-if="event.rights == 'host'") Vous êtes le créateur de cet évènement
						.event--status(ng-if="event.rights == 'member'") Vous participez à cet évènement
						.event--status(ng-if="!event.rights") Vous ne participez pas à cet évènement
						a.btn.btn-small.btn-primary(href="#/event/{{event.id}}").
							#[.fa.fa-info-circle]
							Plus d'informations

			//- Creation d'un évenement
			.calendar--create(ng-if="calendar.calType == 'association'", ng-show="calendar.isSet(3)")
				h4.title Créer un évenement
				.card.event--create
					form
